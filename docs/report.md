# 在线评测系统实验报告

> 课程大作业 2：在线评测系统  
> 姓名：________________　学号：________________　班级：________________  
> 仓库：https://github.com/OverMoon64/online-judge-system

## 摘要

本项目实现了一个面向课程本地验收的在线评测系统。后端采用 FastAPI、SQLAlchemy Async 与 SQLite，前端采用 Streamlit；系统提供用户与角色管理、题目管理、动态语言配置、异步代码评测、提交检索与重测、公开日志与访问审计，以及基于 OpenAI-compatible Chat Completions 协议的可取消 AI 智能命题流程。所有后端路由均为异步接口，响应统一为 `{code, msg, data}`，并根据课程契约返回真实 HTTP 状态码。

判题器在独立临时目录中以参数数组启动编译器或解释器，不使用 `shell=True`。运行期间同时限制时间、递归统计进程树内存、限制输出大小，并在超时或取消后终止整个进程组。系统支持 AC、WA、TLE、MLE、RE、CE 和 UNK，按每个通过测试点 10 分计分。

AI 模块把命题拆分为需求分析、题面与解法、边界测试点、双语言交叉试运行与答案校准等阶段，持续报告进度，统计 Token 与费用，并在保存前通过 Pydantic 结构校验和受限判题器试跑参考解法。API Key 仅加密保存在忽略提交的本机配置文件中，不进入数据库、日志或响应，服务重启后可继续使用。

## 一、需求与评分点映射

项目以课程当前实验说明、API 契约和评分标准为准，实现基础模块 30 分、AI 智能命题 10 分、代码规范 5 分和实验报告 5 分。评分点与实现的对应关系如下。

| 模块 | 主要实现 | 验证方式 |
| --- | --- | --- |
| 用户与权限 | 签名 Session Cookie、bcrypt、user/admin/banned | 接口测试与人工流程 |
| 题目与语言 | 登录用户可新增/编辑/注册语言，管理员可删除 | 校验与权限测试 |
| 异步评测 | Python/C++14、七种结果、逐点得分 | Linux 真实子进程测试 |
| 提交与统计 | 筛选、分页、限流、重测、通过数去重 | 接口测试 |
| 日志与审计 | 可见性矩阵、成功/拒绝访问记录 | 访问矩阵测试 |
| Streamlit | 用户、题目、提交、日志、AI 页面组 | AppTest/浏览器验收 |
| AI 命题 | 配置、任务、取消、Token/费用、试运行 | 本地假提供商测试 |
| 工程质量 | Ruff、pytest、覆盖率、CI、VS Code | 自动化检查 |

## 二、系统架构

系统采用浏览器、REST API、业务服务、异步数据库与隔离执行器五层结构。Streamlit 不直接访问数据库，而是通过独立 HTTP Session 调用 REST API 并保存 Cookie；权限判断始终在后端完成。

核心目录如下：

```text
app/                 FastAPI 路由、模型、服务、判题器与 AI 工作流
frontend/            Streamlit 页面和 REST 客户端
tests/               接口、判题与 AI 自动化测试
scripts/             演示数据与报告构建脚本
docs/                报告源稿与线下验收清单
.github/workflows/   Ubuntu + Python 3.10 持续集成
.vscode/             WSL 解释器、运行、调试和测试配置
```

## 三、API 契约与权限设计

### 3.1 统一响应与错误优先级

成功和失败响应均使用 `{code, msg, data}`。FastAPI/Pydantic 的默认 422 被统一转换为 400，使接口严格符合课程契约。组合条件按 `401 > 403 > 400 > 429 > 409 > 404 > 500` 检查，避免同一请求因实现顺序不同而返回不稳定结果。

### 3.2 认证、角色与封禁

登录成功后只在签名 Session Cookie 中保存用户标识，密码使用 bcrypt 哈希；账户中心支持校验当前密码后安全改密。初始化和系统重置都会创建 `admin / admintestpassword`。普通用户可新增和编辑题目且只能访问本人提交；管理员额外负责题目删除与日志公开策略、语言、角色、全部提交和访问审计；被封禁用户即使持有旧会话也会在依赖层被拒绝。角色变更会拒绝降级或封禁系统中的最后一名管理员，防止误操作导致后台和重置入口永久失去管理权限。

### 3.3 日志可见性与审计

题目支持公开或隐藏测试点日志。管理员始终可查看，普通用户可查看本人提交；题目开启公开后，其他已登录用户也可查看。系统记录资源存在前提下的成功访问和权限拒绝，不记录未登录、参数错误与不存在资源。后台把用户/题目 ID 映射为名称，提供允许、拒绝、涉及用户汇总和最近访问卡片，使权限矩阵可以现场直观核对。

## 四、判题器关键实现

提交接口先写入 `pending` 并立即返回，再由后台任务逐测试点评测。每次提交使用独立临时目录；C++14 先在限制时间内编译，Python 直接解释执行。命令模板会检查占位符、可执行程序白名单和参数形式，最终使用参数数组启动，不经 Shell 解析。

运行阶段并行完成：

1. 等待进程结束并执行硬超时；
2. 使用 psutil 递归统计进程树 RSS；
3. 限制标准输出与错误输出大小；
4. 超时、超内存或取消时终止整个 Linux 进程组；
5. 回收子进程并删除临时目录。

输出比对逐行删除行末空格，并忽略最终多余换行，但保留有意义的前导空格。这样既符合常见 OJ 习惯，也不会把缩进不同的内容误判为相同。

## 五、提交、统计与重置

提交列表支持用户、题目、状态筛选和分页。普通用户只能看到本人提交，管理员可以查看所有记录。每个用户每分钟最多提交 3 次，限流检查位于资源存在性检查之前，以符合错误优先级。重新评测复用同一提交 ID，重置状态与逐点结果后重新进入后台队列。

用户详情统计提交总数与通过题数；通过题数按题目去重，避免同一题多次 AC 被重复计算。系统重置会先取消全部判题和 AI 后台任务，再清空业务数据、重建内置语言和初始管理员，并让当前会话退出。

## 六、AI 智能命题

模型设置包括配置名、兼容接口 URL、模型名、API Key、输入/输出单价、计价单位和币种。每位用户可以保存并选择多个模型；完整配置使用由 `OJ_SESSION_SECRET` 派生的密钥加密写入本机文件，服务重启后无需重新输入。API、数据库和日志只返回是否已配置，不返回密钥本身；系统重置会删除加密配置。OpenAI-compatible 协议没有统一价格接口，单价由用户按服务商账单规则手动确认和填写，因此兼容任意提供商，也避免过期价格造成误导。

模型名不设白名单，系统支持使用 Bearer API Key 的任意 OpenAI-compatible Chat Completions 地址。配置页明确列出 Qwen、DeepSeek、Kimi 三类专项适配和 OpenAI、GLM、OpenRouter、SiliconFlow 等通用兼容服务；原生 Anthropic、Gemini 与 Azure 特殊鉴权接口不在本项目协议边界内。客户端不再对所有模型强制设置采样温度，解决 Kimi K2.5/K2.6 对固定温度参数校验严格而返回 400 的兼容问题。

AI 任务包含需求分析、题面与参考解法、边界测试点、双语言交叉试运行与答案校准等阶段。难度支持自动选择；系统按最终难度要求入门/简单/中等/困难题至少生成 2/2/3/5 个隐藏测试点，用户也可手动指定 2–10 个。模型请求优先采用 OpenAI-compatible SSE 增量响应，不兼容时回退普通响应。系统按阶段聚合输入、输出、思考、缓存和提供商 total Token；缺少 usage 时明确标记估算。

用户可为每次任务选择自动、低、中、高、最高推理强度。自动档不改变提供商默认设置；百炼 Qwen 根据模型映射到 `enable_thinking/thinking_budget` 或 `reasoning_effort`，DeepSeek 使用官方 `reasoning_effort`，Kimi 官方接口使用 `thinking` 开关并通过提示词表达细分档位。其他兼容服务先尝试标准字段，若接口以 400/422 拒绝可选推理参数，则自动去除参数并按提供商默认值继续，避免协议差异降低命题成功率。Kimi 等把缓存或推理 Token 放在 usage 顶层的响应也会被纳入统计。

模型输出必须通过严格的 Pydantic 题目结构校验，并同时提供 Python 3 与 C++14 参考解法。判题器对 C++ 只编译一次，再让两份程序运行全部样例和隐藏测试点；若逐点规范化输出一致，且至少一个模型原始非空答案与共识结果相符，系统把该答案作为题意与下标定义的安全锚点，并以双解共识校准其余错误或空白答案。双解不一致、均执行失败、无安全锚点或输出超限时禁止自动回填，实际状态、输入、stdout 与错误会进入最多两次的修复反馈；仅一种语言失败时只定向修复该语言。校准不调用模型，因此不增加 Token 或费用。解析器仍容忍模型偶发的字符串控制字符，并归一化常见根结构与解法字段别名。取消接口会传播到正在进行的 httpx 流式请求和本地试运行；通过后由服务端把题目作者固定为实际模型名。

为保证不同 OpenAI-compatible 模型的稳定成功率，最终方案不执行模型生成的压力数据程序，也不把低效探针作为命题完成条件。模型返回完整、紧凑的输入，系统校验测试点数量、重复项、占位符和总长度；不足时只定向修复测试数据。模型填写的通用 1 s / 128 MB 不作为最终值：系统按最终难度与算法结构确定资源限制，只有需求中明确指定的限制会优先保留。新题由服务端按 AI0001、AI0002 顺序分配编号，修改已有题目则保持原 ID。

为增强复杂度区分能力，需求明确出现复杂度或大数据要求时，系统在普通命题成功后尝试生成一个确定性的文件压力点，而不是要求模型编写生成器。系统可从已校验题目的输入契约识别单整数、计数数组、字符串、网格和图模板，在声明上限内生成大规模 `.in`；Python 与 C++ 参考程序均正常结束且输出一致后，才把共识结果写入配对 `.out` 并纳入正式判题。文件通过题目输入契约指纹、长度和 SHA-256 校验绑定，修改相关字段、删除题目或系统重置会同步清理。任何结构识别、执行或一致性失败都只使增强被跳过，不改变 AI 任务的成功状态，也不触发额外模型请求或修复。因此该层提高了暴力解被 TLE 的概率，同时保持原命题成功率；它仍不是算法复杂度的形式化证明，结果必须人工审阅。

对本机最近三次未生成文件点的任务复盘发现，模型分别使用了 `100,000`、LaTeX `\\le` 和 `\\times` 表示数值范围，旧解析器因未归一化这些形式而安全跳过。修复后解析器兼容数字千分位、普通/Unicode/LaTeX 不等号、幂和乘方记法；三份真实旧结果均可重新识别为 `N=75000`、约 180 KB 的数组压力候选。

## 七、前端与交互

Streamlit 前端在屏幕顶部提供账户概览、题库与评测、提交记录、AI 智能命题和后台管理导航，并显示当前用户名、中文角色与退出按钮；入口按角色精简，权限仍由后端最终裁决。题库与在线评测合并在同一工作区，默认先显示可点击的题库，进入题目后连续展示完整题面、限制、样例、代码编辑器和独立提交按钮；历史记录与详情位于单独的“提交记录”页并按每页 5 条分页。编辑器提供行号、Tab 缩进、括号补全、语法高亮、查找替换和 VS Code 快捷键。每条记录保留 AC、WA、TLE、MLE、RE、CE、UNK 或 Pending 的缩写与中文解释，并使用独立颜色；详情可查看源代码和逐测试点结果。

后台语言管理显示 Python 3、C/C++、Java、JavaScript、Ruby 和 Go 的文件扩展名、编译命令、运行命令与所需程序。Python 3 和 C++14 在初始化时内置；其余模板只有在 WSL 安装相应运行时后才能注册使用。命令仍由后端白名单和占位符规则校验，不能通过前端说明绕过安全检查。

前端在 `st.session_state` 中保存原生 `httpx.Client`，而不是每次 rerun 都可能因脚本类重新定义而失效的包装器实例。登录、资料、用户、题目、提交、日志、AI 与管理请求因此复用同一 Cookie 容器；退出会同时调用后端 logout 并清理本地 Cookie。通信只使用统一的 `OJ_API_BASE_URL`，登录过期、后端未启动、接口错误、空数据和窄屏均有明确反馈。

AI 模块默认打开“智能命题”，允许从多个已保存配置中选择模型；难度可自动判断，模型配置页展示累计输入/输出 Token 与人民币费用。任务完成后展示系统题号、最终难度、资源限制、题目、参考解法和测试点；发生答案校准时提供修改前后明细。用户确认后才载入最终题目并跳转新增表单。

![Streamlit 在线评测系统 AI 智能命题](assets/ui-overview.png)

图 1　顶部导航、AI 智能命题表单与自动难度选项

## 八、测试设计与边界结果

自动化测试覆盖字段校验、错误优先级、会话、角色与封禁、前端 Cookie 跨 rerun/退出回归、题目 CRUD、语言命令安全、筛选分页、限流、统计、日志可见性、访问审计、重置与 AI 工作流。Linux 集成测试真实执行 Python 和 C++，覆盖 AC、WA、RE、CE、TLE、MLE、空白字符比对、重新评测和进程清理。

最终在 Ubuntu 22.04、Python 3.10.12、G++ 11.4.0 环境运行 73 个测试，结果为 73 passed；后端总行覆盖率为 90.10%，超过 CI 设置的 85% 门槛，Ruff 静态检查和格式检查均通过。实际启动 Uvicorn 与 Streamlit 后，健康接口、API 文档和前端入口均返回 200；Streamlit AppTest 通过。真实浏览器的管理员、普通用户、窄屏与控制台检查留待最终人工验收，完成前不在报告中声称已经通过。

AI 测试使用本地假提供商，覆盖成功、非法 JSON、超时/鉴权失败、Token 与费用、任务取消、自动难度、任务级推理强度、Qwen/DeepSeek/Kimi 参数映射、未知兼容服务参数回退、按难度确定测试点数量、系统资源覆盖、顺序 ID、斐波那契错误答案的双语言校准，以及文件压力点的模板推断、常见约束记法、哈希防篡改、降级和生命周期。集成测试确认普通小点可通过的 O(n²) 数组程序会在大规模文件点触发 TLE，并真实注册 C17 后使用 GCC 完成 AC 判题；不带复杂度要求和无法识别模板的任务都保持两次模型调用并正常完成。CI 在 Ubuntu、Python 3.10 和 G++ 环境执行 Ruff、格式检查与 pytest，并设置 85% 覆盖率门槛。

## 九、安全边界

本项目的进程组、资源限制、命令白名单和临时目录可以满足课程本地验收，但不能等同于内核级强隔离沙箱。若部署到公网，应把判题执行迁移到容器或专用沙箱，并增加只读根文件系统、用户/网络命名空间、seccomp、cgroup、作业队列、审计持久化与反向代理安全配置。

## 十、总结

本实验把异步 Web API、数据库、操作系统进程管理、权限审计和大模型工作流整合为一个可运行、可测试、可演示的系统。实现的重点不是单纯“运行代码”，而是在明确 API 契约下正确处理并发、资源回收、权限优先级、数据一致性和失败路径。项目附带 VS Code Remote WSL 配置、演示种子数据、线下验收清单和持续集成流程，便于重复验证。

## AI 使用说明

本项目开发过程中使用了 AI 辅助进行需求梳理、代码实现、测试设计与报告排版。所有生成内容均通过静态检查、自动化测试或人工审阅验证；AI 模型密钥不写入仓库。验收演示中的 AI 命题结果也必须由管理员人工检查后才能保存为正式题目。
